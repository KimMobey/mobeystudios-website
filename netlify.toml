[build]
  publish = "public"
  command = "hugo"

[build.environment]
  HUGO_VERSION = "0.160.0"

# Admin UI lives at /admin/ — not linked from the public site.
# Runs entirely in the browser via the File System Access API (Chrome/Edge/Arc).

# Production deploys restricted to main branch only.
# This conserves Netlify free tier credits (300/month, 15 per deploy).
# Do not remove this restriction — pushes to any other branch must not trigger deploys.
[[context.branch-deploy]]
  command = ""

[context.production]
  branch = "main"

[context.production.environment]
  HUGO_ENV = "production"
